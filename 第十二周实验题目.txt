一、综合实验题
       学生信息管理系统
       数据库字段：姓名、学号、专业、性别、出生年月、手机号码、家庭住址、该学生本人相片文件名
	           相片文件夹
    　功能要求
	1. 首先实现学生基本信息浏览
	通过点击数据库浏览工具栏上的按钮实现信息的浏览，浏览的同时，学生的相片也显示在图像控件上，还能通过点击“放大显示”来在单独的对话框上居中显示当前学生的照片。	　
            OnPaint(),
            MoveNext()
            MovePrev()
            MoveFirst()
            MoveLast()
            IsBOF()
            IsEOF()
            GetFieldValue()  　或　GetDlgItemText(IDC_EDIT3, str)

	2. 数据库管理——增加、删除、修改：
		AddNew(),Edit()--Update()  //进入加新、编辑状态，更新
		Delete()--MoveNext()
		通过弹出对话框进行

	3. 数据库查询排序
		m_strSort, 
                m_strFilter
                Requery()
		
	*4. 进一步的完备（选作题）：
		1）通过List Control 呈现查询结果
		2）自动注册数据源
		3）通过SQL语句获取查询结果

二、实验题
建立含5张相片的数据库，内含5条记录，每条记录以相片文件名作为唯一字段，５张相片放在某个文件夹中。
编程，实现相片的浏览功能。
实现步骤：
        １.　重载“记录”菜单下各子菜单项的事件处理函数
        ２.　在这些事件处理函数中添加显示图像的代码(讲解视频中讲到这里)
        ３.　改进为在这些事件处理函数中仅读取文件名并强制重绘，而将显示图像代码放在OnPaint中