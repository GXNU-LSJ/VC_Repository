1.  在实现学生相片浏览的基础上，　编程实现相片的放大居中显示，　即当点击“放大显示”按钮时，弹出单独的一个对话框，在其上的图片控件上显示相片。
   提示： 构造对话框后，在弹出对话框之前，传文件名给对话框，在对话框的OnPaint中画图 

2.  建立基于对话框的程序，使得在程序一运行时，图片就直接显示在对话框上，图片文件的路径名
直接写在代码中。

3.
1.  数据库记录的增加
     AddNew()——开启增加新记录的状态
     Update()——更新，使增加新记录生效    

2.  数据库记录的修改
     Edit()——开启编辑当前记录的状态
     Update()——更新，使编辑生效   

3.  数据库记录的删除
     Delete()——删除当前记录　
　  移动记录集指针(MovePrev, MoveNext, )即使删除生效